export function Step2() {
  return (
    <div className="space-y-2">
      <div className="text-lg font-semibold">Step 2 — Create imprint (if tumor available)</div>
      <div className="text-sm text-slate-600">
        Заглушка. Здесь будет загрузка FASTQ и подсветка LOH/CNV/SNV.
      </div>
    </div>
  );
}